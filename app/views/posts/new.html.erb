<%= form_for @post do |f|%>
  <div><%= f.label :title %>
  <%= f.text_field :title %></div>
  <div><%= f.label :body %>
  <%= f.text_area :body, rows: 10, cols: 80 %></div>
  <div> <%= f.select :category, options_for_select(["life", "discover","humor", "travel", "good"]), prompt: "Category"%></div>
  <%= f.submit %>
<% end %>